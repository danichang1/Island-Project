using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GemChecks : MonoBehaviour
{
    static public bool hasFish = false;
    static public bool fishGem = false;
    static public bool mountainGem = false;
    static public bool miningGem = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
